{
  "name": "@lokshineats/api",
  "version": "0.1.0",
  "private": true,
  "description": "LokshinEats REST API — server-authoritative orders, payments, and wallets over Firestore.",
  "main": "dist/index.js",
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "pretest:integration": "node scripts/free-emulator-port.mjs 8080 9099",
    "test:integration": "firebase emulators:exec --project lokshineats-test --only firestore,auth --config ../firebase.json \"vitest run --config vitest.integration.config.ts\"",
    "test:all": "npm run test && npm run test:integration",
    "emulator": "firebase emulators:start --project lokshineats-test --only firestore,auth --config ../firebase.json"
  },
  "dependencies": {
    "compression": "^1.7.5",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "firebase-admin": "^13.0.2",
    "helmet": "^8.0.0",
    "pino": "^9.6.0",
    "pino-http": "^10.4.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "@types/compression": "^1.7.5",
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.5",
    "@types/supertest": "^6.0.2",
    "@vercel/node": "^12.0.2",
    "firebase": "^12.18.0",
    "firebase-tools": "^15.29.0",
    "supertest": "^7.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
