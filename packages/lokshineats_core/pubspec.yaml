name: lokshineats_core
description: "Shared foundation for the LokshinEats apps — API client, authentication, theme and common widgets."
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter

  flutter_riverpod: ^3.4.3
  http: ^1.6.0
  intl: ^0.20.3
  firebase_core: ^4.14.0
  firebase_auth: ^6.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
